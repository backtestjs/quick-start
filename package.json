{
  "name": "@backtestjs/quick-start",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "keywords": [
    "backtestjs",
    "boilerplate",
    "quick-start",
    "project-template"
  ],
  "scripts": {
    "start": "node -r ts-node/register --env-file=.env src/main.ts",
    "dev": "node -r ts-node/register --env-file=.env src/main.ts",
    "build": "tsc",
    "prejs": "npm run build",
    "js": "node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build",
    "postinstall": "npm run build"
  },
  "prisma": {
    "schema": "./node_modules/@backtestjs/framework/prisma/schema.prisma"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "dayjs": "^1.11.13",
    "technicalindicators": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "prisma": "^5.21.0",
    "ts-node": "^10.9.2",
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
