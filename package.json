{
  "name": "test-project",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main",
  "typings": "dist/main",
  "keywords": [
    "backtestjs",
    "backtest",
    "test",
    "example"
  ],
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "cd dist && node --env-file=../.env main.js",
    "predemo-js": "npm run build",
    "dev": "node -r ts-node/register --env-file=.env main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "prisma": {
    "schema": "./node_modules/@backtestjs/core/prisma/schema.prisma"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "dayjs": "^1.11.13",
    "technicalindicators": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "prisma": "^5.21.0",
    "ts-node": "^10.9.2",
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
